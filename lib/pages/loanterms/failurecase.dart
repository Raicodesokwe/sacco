import 'package:flutter/material.dart';

class FailureCase extends StatelessWidget {
  const FailureCase({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}