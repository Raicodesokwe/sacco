import 'package:flutter/material.dart';

class SuccessCase extends StatelessWidget {
  const SuccessCase({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}